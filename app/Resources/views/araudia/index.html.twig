{% extends 'base.html.twig' %}

{% block menua %}


    {{ parent() }}

    <ul class="list-inline pull-right" role="tablist">
        <li><a href="{{ path('araudia_new') }}" class="btn btn-primary">Arau berria</a></li>
    </ul>
{% endblock %}


{% block body %}
    <h1>Araudiak</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Kodea</th>
                <th>Araua</th>
            </tr>
        </thead>
        <tbody>
        {% for araudium in araudias %}
            <tr>
                {#<td><a href="{{ path('araudia_show', { 'id': araudium.id }) }}">{{ araudium.kodea }}</a></td>#}
                <td><a href="{{ path('araudia_edit', { 'id': araudium.id }) }}">{{ araudium.kodea }}</a></td>
                <td>{{ araudium.arauaeu }}</td>
                {#<td>#}
                    {#<ul>#}
                        {#<li>#}
                            {#<a href="{{ path('araudia_show', { 'id': araudium.id }) }}">show</a>#}
                        {#</li>#}
                        {#<li>#}
                            {#<a href="{{ path('araudia_edit', { 'id': araudium.id }) }}">edit</a>#}
                        {#</li>#}
                    {#</ul>#}
                {#</td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
