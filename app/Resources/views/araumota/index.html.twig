{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Arau-motak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('araumota_new') }}" class="fa fa-plus"> Arau-mota berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

{% block body %}
    {#<h1>Arau-motak</h1>#}

    <table class="table">
        <thead>
            <tr>
                <th>Kodea</th>
                <th>Motaeu</th>
                {#<th>Motaes</th>#}
                {#<th>Id</th>#}
                {#<th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for araumotum in araumotas %}
            <tr>
                {#<td><a href="{{ path('araumota_show', { 'id': araumotum.id }) }}">{{ araumotum.kodea }}</a></td>#}
                {#<td><a href="{{ path('araumota_edit', { 'id': araumotum.id }) }}">{{ araumotum.kodea }}</a></td>#}
                <td>{{ araumotum.kodea }}</td>
                <td>{{ araumotum.motaeu }}</td>
                {#<td>{{ araumotum.motaes }}</td>#}
                {#<td>{{ araumotum.id }}</td>#}
                <td>
                    {#<ul>#}
                        {#<li>#}
                            {#<a href="{{ path('araumota_show', { 'id': araumotum.id }) }}">show</a>#}
                        {#</li>#}
                        {#<li>#}
                            <a href="{{ path('araumota_edit', { 'id': araumotum.id }) }}" class="fa fa-pencil"></a>
                        {#</li>#}
                    {#</ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
