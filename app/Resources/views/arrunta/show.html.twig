{% extends 'base.html.twig' %}

{% block body %}
    <h1>Arrunta</h1>

    <table>
        <tbody>
            <tr>
                <th>Epeaeu</th>
                <td>{{ arruntum.epeaeu }}</td>
            </tr>
            <tr>
                <th>Epeaes</th>
                <td>{{ arruntum.epeaes }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ arruntum.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('arrunta_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('arrunta_edit', { 'id': arruntum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
