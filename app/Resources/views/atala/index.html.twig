{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Atalak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('atala_new') }}">Atal berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

{% block body %}
    {#<h1>Atalak</h1>#}

    <table class="table">
        <thead>
            <tr>
                <th>Kodea</th>
                <th>Izenburua</th>
            </tr>
        </thead>
        <tbody>
        {% for atala in atalas %}
            <tr>
                <td><a href="{{ path('atala_edit', { 'id': atala.id }) }}">
                        {{ atala.ordenantza.kodea }}.{{ atala.kodea }}</a></td>
                <td>{{ atala.izenburuaeu }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
