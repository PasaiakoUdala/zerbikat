{% extends 'base.html.twig' %}

{% block body %}
    <h1>Aurreikusi</h1>

    <table>
        <tbody>
            <tr>
                <th>Epeaeu</th>
                <td>{{ aurreikusi.epeaeu }}</td>
            </tr>
            <tr>
                <th>Epeaes</th>
                <td>{{ aurreikusi.epeaes }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ aurreikusi.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('aurreikusi_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('aurreikusi_edit', { 'id': aurreikusi.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
