{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">{% if app.user.udala.eremuak %}{{ app.user.udala.eremuak.besteak1labeleu }}{% endif %}</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('besteak1_new') }}" class="fa fa-plus">{% if app.user.udala.eremuak %} {{ app.user.udala.eremuak.besteak1labeleu }}{% else %}Sarrera{% endif %} berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

{% block body %}
    {#<h1>{{ app.user.udala.eremuak.besteak1labeleu }} zerrenda</h1>#}
    <table class="table">
        <thead>
            <tr>
                <th>Kodea</th>
                <th>Izenburuaeu</th>
                <th></th>
                {#<th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for besteak1 in besteak1s %}
            <tr>
                <td>{{ besteak1.kodea }}</td>
                <td>{{ besteak1.izenburuaeu }}</td>
                <td>
                    {#<ul>#}
                        {#<li>#}
                            <a href="{{ path('besteak1_edit', { 'id': besteak1.id }) }}" class="fa fa-pencil"></a>
                        {#</li>#}
                    {#</ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
