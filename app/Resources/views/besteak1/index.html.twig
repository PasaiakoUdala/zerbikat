{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">{% if app.user.udala.eremuak %}{{ app.user.udala.eremuak.besteak1labeleu }}{% endif %}</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('besteak1_new') }}" class="fa fa-plus">{% if app.user.udala.eremuak %} {{ app.user.udala.eremuak.besteak1labeleu }}{% else %} Sarrera{% endif %} berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

{% block body %}
    <table class="table">
        <thead>
            <tr>
                {% if is_granted('ROLE_SUPER_ADMIN') %}<th>{{ 'messages.udala'|trans }}</th>{% endif %}
                <th>{{ 'messages.kodea'|trans }}</th>
                <th>{{ 'messages.izenburua'|trans }}</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for besteak1 in besteak1s %}
            <tr>
                {% if is_granted('ROLE_SUPER_ADMIN') %}<td>{{ besteak1.udala }}</td>{% endif %}
                <td>{{ besteak1.kodea }}</td>
                <td>{{ besteak1.izenburuaeu }}</td>
                <td NOWRAP class="text-right">
                    <ul class="list-inline">
                        <li><a href="{{ path('besteak1_edit', { 'id': besteak1.id }) }}" class="fa fa-pencil"></a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                            {#<li>{{ form_start(deleteforms[besteak1.id],{'attr': {'id': besteak1.id}}) }}#}
                                {#<a href="#" onclick="document.getElementById({{ besteak1.id }}).submit();" ><i class="fa fa-trash text-danger"></i></a>#}
                                {#{{ form_end(deleteforms[besteak1.id]) }}</li>#}
                            <li>{{ form_start(deleteforms[besteak1.id],{'attr': {'id': besteak1.id}}) }}
                                <a href="#" onclick="ezabatu({{ besteak1.id }});" ><i class="fa fa-trash text-danger"></i></a>
                                {{ form_end(deleteforms[besteak1.id]) }}</li>
                        {% endif %}

                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="text-center">
        {% if besteak1s is not empty %}
            {% if pager.haveToPaginate %}
                {{ pagerfanta(pager, 'twitter_bootstrap3_translated', {'routeName': 'besteak1_index_paginated'}) }}
            {% endif %}
        {% else %}
            <p>Ez da emaitzarik topatu</p>
        {% endif %}
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript">
        function ezabatu (id)
        {
            var r = confirm("Besteak1 EZABATU. Ziur zaude?");
            if (r == true)
            {
                document.getElementById(id).submit()
            }else
            {
            }
        };
    </script>
{% endblock %}