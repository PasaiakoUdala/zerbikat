{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">{% if app.user.udala.eremuak %}{{ app.user.udala.eremuak.doklagunlabeleu }}{% endif %}</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('doklagun_new') }}" class="fa fa-plus"> Sarrera berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

{% block body %}
    {#<h1>{{ app.user.udala.eremuak.doklagunlabeleu }}</h1>#}
    <table class="table">
        <thead>
            <tr>
                {#<th>Id</th>#}
                <th>Kodea</th>
                <th>Deskribapenaeu</th>
                {#<th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for doklagun in doklaguns %}
            <tr>
                <td>{{ doklagun.kodea }}</td>
                <td>{{ doklagun.deskribapenaeu }}</td>
                <td>
                    {#<ul>#}
                        {#<li>#}
                            <a href="{{ path('doklagun_edit', { 'id': doklagun.id }) }}" class="fa fa-pencil"></a>
                        {#</li>#}
                    {#</ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
