{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Agiriak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('dokumentazioa_new') }}" class="fa fa-plus"> Agiri berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
{% block body %}
    {#<h1>Agiriak</h1>#}
    <table class="table">
        <thead>
            <tr>
                <th>Kodea</th>
                <th>Deskribapenaeu</th>
                {#<th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for dokumentazioa in dokumentazioas %}
            <tr>
                <td>{{ dokumentazioa.kodea }}</td>
                <td>{{ dokumentazioa.deskribapenaeu }}</td>
                <td>
                    {#<ul>#}
                        {#<li>#}
                            <a href="{{ path('dokumentazioa_edit', { 'id': dokumentazioa.id }) }}" class="fa fa-pencil"></a>
                        {#</li>#}
                    {#</ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
