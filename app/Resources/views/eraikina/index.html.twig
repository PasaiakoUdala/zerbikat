{% extends 'base.html.twig' %}

{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Eraikinak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('eraikina_new') }}">Eraikin berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>Izena</th>
                <th>Koordenadak</th>
                {#<th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for eraikina in eraikinas %}
            <tr>
                <td><a href="{{ path('eraikina_edit', { 'id': eraikina.id }) }}">{{ eraikina.izena }}</a></td>
                <td>({{ eraikina.longitudea }}, {{ eraikina.latitudea }})</td>
                {#<td>#}
                    {#<ul>#}
                        {#<li>#}
                            {#<a href="{{ path('eraikina_edit', { 'id': eraikina.id }) }}">edit</a>#}
                        {#</li>#}
                    {#</ul>#}
                {#</td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
