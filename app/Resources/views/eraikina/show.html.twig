{% extends 'base.html.twig' %}

{% block body %}
    <h1>Eraikina</h1>

    <table>
        <tbody>
            <tr>
                <th>Izena</th>
                <td>{{ eraikina.izena }}</td>
            </tr>
            <tr>
                <th>Longitudea</th>
                <td>{{ eraikina.longitudea }}</td>
            </tr>
            <tr>
                <th>Latitudea</th>
                <td>{{ eraikina.latitudea }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ eraikina.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('eraikina_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('eraikina_edit', { 'id': eraikina.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
