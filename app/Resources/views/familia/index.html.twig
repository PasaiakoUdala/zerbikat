{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Familiak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('familia_new') }}" class="fa fa-plus"> Familia berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
{% block body %}
    {#<h1>Familiak</h1>#}
    <table class="table">
        <thead>
            <tr>
                {#<th>Id</th>#}
                <th>Familiaeu</th>
                {#<th>Familiaes</th>#}
                <th>Deskribapenaeu</th>
                {#<th>Deskribapenaes</th>#}
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for familium in familias %}
            <tr>
                <td>{{ familium.familiaeu }}</td>
                <td>{{ familium.deskribapenaeu }}</td>
                <td><a href="{{ path('familia_edit', { 'id': familium.id }) }}" class="fa fa-pencil"></a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
