{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Kaleak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('kalea_new') }}" class="fa fa-plus"> Kale berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>Izena</th>
                {#<th>Google</th>#}
                {#<th>Id</th>#}
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for kalea in kaleas %}
            <tr>
                <td>{{ kalea.izena }}</td>
                {#<td>{{ kalea.google }}</td>#}
                {#<td>{{ kalea.id }}</td>#}
                <td>
                    {#<ul>#}
                        {#<li>#}
                            {#<a href="{{ path('kalea_show', { 'id': kalea.id }) }}">show</a>#}
                        {#</li>#}
                        {#<li>#}
                            <a href="{{ path('kalea_edit', { 'id': kalea.id }) }}" class="fa fa-pencil"></a>
                        {#</li>#}
                    {#</ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{#<div class="fitxaMenua">#}
    {#<ul>#}
        {#<li>#}
            {#<a href="{{ path('kalea_new') }}" class="btn btn-primary">Kale berria</a>#}
        {#</li>#}
    {#</ul>#}
{#</div>#}
{% endblock %}
