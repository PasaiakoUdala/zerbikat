{% extends 'base.html.twig' %}

{% block body %}
    <h1>Kalea</h1>

    <table>
        <tbody>
            <tr>
                <th>Izena</th>
                <td>{{ kalea.izena }}</td>
            </tr>
            <tr>
                <th>Google</th>
                <td>{{ kalea.google }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ kalea.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('kalea_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('kalea_edit', { 'id': kalea.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
