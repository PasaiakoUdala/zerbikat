{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Kontzeptu-motak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('kontzeptumota_new') }}" >Mota berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>Motaeu</th>
                {#<th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for kontzeptumotum in kontzeptumotas %}
            <tr>
                <td><a href="{{ path('kontzeptumota_edit', { 'id': kontzeptumotum.id }) }}">{{ kontzeptumotum.motaeu }}</a></td>
                {#<td>#}
                    {#<ul>#}
                        {#<li>#}
                            {#<a href="{{ path('kontzeptumota_edit', { 'id': kontzeptumotum.id }) }}">edit</a>#}
                        {#</li>#}
                    {#</ul>#}
                {#</td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
