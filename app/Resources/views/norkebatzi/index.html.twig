{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Ebazleak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('norkebatzi_new') }}" class="fa fa-plus"> Ebazle berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
{% block body %}
    <table class="table">
        <thead>
            <tr>
                {% if is_granted('ROLE_SUPER_ADMIN') %}<th>Udala</th>{% endif %}
                <th>Nork</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for norkebatzi in norkebatzis %}
            <tr>
                {% if is_granted('ROLE_SUPER_ADMIN') %}<td>{{ norkebatzi.udala }}</td>{% endif %}
                <td>{{ norkebatzi.norkeu }}</td>
                <td NOWRAP class="text-right">
                    <ul class="list-inline">
                            <li><a href="{{ path('norkebatzi_edit', { 'id': norkebatzi.id }) }}" class="fa fa-pencil"></a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                            {#<li>{{ form_start(deleteforms[norkebatzi.id],{'attr': {'id': norkebatzi.id}}) }}#}
                                {#<a href="#" onclick="document.getElementById({{ norkebatzi.id }}).submit();" ><i class="fa fa-trash text-danger"></i></a>#}
                                {#{{ form_end(deleteforms[norkebatzi.id]) }}</li>#}
                            <li>{{ form_start(deleteforms[norkebatzi.id],{'attr': {'id': norkebatzi.id}}) }}
                                <a href="#" onclick="ezabatu({{ norkebatzi.id }});" ><i class="fa fa-trash text-danger"></i></a>
                                {{ form_end(deleteforms[norkebatzi.id]) }}</li>
                        {% endif %}
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="text-center">
        {% if norkebatzis is not empty %}
            {% if pager.haveToPaginate %}
                {{ pagerfanta(pager, 'twitter_bootstrap3_translated', {'routeName': 'norkebatzi_index_paginated'}) }}
            {% endif %}
        {% else %}
            <p>Ez da emaitzarik topatu</p>
        {% endif %}
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript">
        function ezabatu (id)
        {
            var r = confirm("Ebazlea EZABATU. Ziur zaude?");
            if (r == true)
            {
                document.getElementById(id).submit()
            }else
            {
            }
        };
    </script>
{% endblock %}