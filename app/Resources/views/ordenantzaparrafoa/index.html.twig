{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Ordenantzen parrafoak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('ordenantzaparrafoa_new') }}" class="fa fa-plus"> Parrafo berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>Testuaeu</th>
                <th>Testuaes</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for ordenantzaparrafoa in ordenantzaparrafoas %}
            <tr>
                <td>{{ ordenantzaparrafoa.testuaeu |raw}}</td>
                <td>{{ ordenantzaparrafoa.testuaes |raw}}</td>
                <td><a href="{{ path('ordenantzaparrafoa_edit', { 'id': ordenantzaparrafoa.id }) }}" class="fa fa-pencil"></a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
