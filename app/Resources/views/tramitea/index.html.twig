{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Tramiteak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('tramitea_new') }}" >Tramite berria</a></li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>Tramitea</th>
                {#<th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for tramitea in tramiteas %}
            <tr>
                <td><a href="{{ path('tramitea_edit', { 'id': tramitea.id }) }}">{{ tramitea.tramiteaeu }}</a></td>
                 {#<td>#}
                    {#<ul>#}
                        {#<li>#}
                            {#<a href="{{ path('tramitea_edit', { 'id': tramitea.id }) }}">edit</a>#}
                        {#</li>#}
                    {#</ul>#}
                {#</td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
