{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tramitea</h1>

    <table>
        <tbody>
            <tr>
                <th>Tramiteaeu</th>
                <td>{{ tramitea.tramiteaeu }}</td>
            </tr>
            <tr>
                <th>Tramiteaes</th>
                <td>{{ tramitea.tramiteaes }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ tramitea.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tramitea_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tramitea_edit', { 'id': tramitea.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
