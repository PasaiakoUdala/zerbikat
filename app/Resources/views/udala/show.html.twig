{% extends 'base.html.twig' %}
{% block menua %}
    {{ parent() }}
    <nav class="navbar navbar-default navbar-lower" data-spy="affix" data-offset-top="5">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand">Udalak</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse navbar-right">
                <ul class="nav navbar-nav">
                    {% if is_granted('ROLE_SUPER_ADMIN') %}<li><a href="{{ path('udala_index') }}" class="fa fa-undo"> Zerrendara itzuli</a></li>{% endif %}
                    {% if is_granted('ROLE_ADMIN') %}<li><a href="{{ path('udala_edit', { 'id': udala.id }) }}" class="fa fa-pencil"> Aldatu</a></li>{% endif %}
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
{#{% block menua %}#}
    {#{{ parent() }}#}
    {#<ul class="list-inline pull-right" role="tablist">#}
        {#{% if is_granted('ROLE_SUPER_ADMIN') %}<li><a href="{{ path('udala_index') }}" class="btn btn-primary">Zerrendara itzuli</a></li>{% endif %}#}
        {#{% if is_granted('ROLE_ADMIN') %}<li><a href="{{ path('udala_edit', { 'id': udala.id }) }}" class="btn btn-primary">Aldatu</a></li>{% endif %}#}
    {#</ul>#}
{#{% endblock %}#}

{% block body %}
    <table class="table table-bordered">
        <tbody>
        <tr>
            <th class="col-md-3 text-center">
                <img src='{{ udala.logoa }}' />
            </th>
            <th>
                <h4>{{ udala.izenaeu }} - {{ udala.izendapenaeu }} - {{ udala.ifk }}</h4>
                <h5>{{ udala.izenaeu }} - {{ udala.izendapenaes }} - {{ udala.ifk }}</h5>
            </th>
        </tr>
        {#<tr>#}
        {#<td class="col-md-6">{{ udala.lopdeu }}</td>#}
        {#<td class="col-md-6">{{ udala.lopdes }}</td>#}
        {#</tr>#}

        </tbody>
    </table>


{% endblock %}